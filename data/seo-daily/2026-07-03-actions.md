# SEO 自動優化 2026-07-03

## ① 昨日賭注勝負
無。這是本 pipeline 首次執行（`data/seo-daily/` 只有 2026-07-02、2026-07-03 兩份 JSON，且 repo 中沒有更早的 `-actions.md`），沒有前一天的賭注可驗證。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-03.json`（GSC 區間 2026-06-22..2026-06-29，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：46 人（7 天視窗，無單日對照基準，先記錄本次數值供明日比較）
- GSC 總覽：clicks 69、impressions 4052、position 17.3
- `highImpZeroClick` 最大訊號：`uk-heatwave-red-warning-40c`（英國熱浪紅色警戒）在「嚴重高溫警告」排第 4.3 名、被看到 130 次、0 點擊——訊號本身極強，但該文 `publishDate` 為 2026-06-23，距今 10 天，已超出站規「時效稿 7 天內」的優化窗口（時效稿排名窗口短，改標題/內鏈邊際效益低），故本次**不動這篇**，留給窗口內的稿件。
- 次強訊號是一組「髖關節痛」長青科普文的 `pageQueryCross`／`highImpZeroClick` 疊加：
  - `hip-pain-symptoms-causes-treatment`（wp-426）：「髖關節痛」「髖 關節 痛 原因」「髖 關節 痛 治療」等查詢合計約 60+ 次曝光、0 點擊，排名普遍在 70–90 名，與文章主題（髖關節痛症狀/原因/治療）高度相關但排名極差。
  - `hip-pain-which-specialist`（wp-428）：同主題群，已與 wp-426 共用「髖關節退化」標籤。
  - 另有兩個 WordPress 舊版中文網址（URL-encoded，指向同一批文章）也在榜上，但那是舊永久連結經 301 已轉址、Google 索引尚未完全汰換的殘留，非可行動項。
- 檢查 `relatedArticles()`（`src/utils/content.ts`）評分邏輯：同分類 +2、同子分類 +1、**共用 tag 逐字比對**才加分。髖關節痛文章群（wp-426／wp-428／wp-432／wp-436／wp-438／appi-news-97）彼此標籤幾乎各自獨立措辭（如「髖關節痛原因」vs「髖關節疼痛」vs「髖關節手術」），導致「延伸閱讀」互相導流很弱——這正是站規指出的「內鏈入口密度不足」問題，屬分類/相關文章模組層級的基礎設施問題，不涉及事實內容。
- `index.coverage`：僅 10 個代表性頁面回傳，皆為 `Submitted and indexed`，無 Discovered/Crawled-not-indexed 訊號可處理。

## ③ 今天做了什麼

只動 **2 個檔**，皆為 `tags` 陣列新增一個既有詞彙（不刪除任何既有 tag、不動 title/description/內文）：

- `src/content/articles/wp-426.md`：`tags` 追加 `"髖關節痛"`
- `src/content/articles/wp-428.md`：`tags` 追加 `"髖關節痛"`

**理由**：`"髖關節痛"` 是查詢資料中曝光量最高、排名最差的精確詞（單日 GSC 快照中 `髖關節痛`／`髖 關節 痛 原因`／`髖 關節 痛 治療` 合計數十次曝光、0 點擊、排名 70–90+），且這兩篇文章主題與此詞高度貼合。新增這個共用 tag 會提高 `relatedArticles()` 的標籤重疊分數，讓這兩篇（以及未來新文章若用到同一詞彙）在彼此的「延伸閱讀」區塊更容易互相導流，屬於站規允許的「相關文章模組」內鏈優化，不觸及標題、meta description 或內文事實。

**預期效果**：兩篇文章互相導流增強，內鏈權重集中，有機會逐步墊高「髖關節痛」相關查詢的排名（效果需觀察數週，GSC 本身也有 2–3 天延遲，短期內不會看到明顯位移）。

**驗證**：`pnpm install --frozen-lockfile` → `pnpm build`（0 errors）→ `pnpm check:links`（2131 個 HTML、105589 條內部連結，0 壞連結）全綠。

**部署**：commit 後 push 到 `main`，交給既有 `.github/workflows/deploy.yml` 自動建置部署；部署完成後 IndexNow 由 workflow 自動推播（本次未新增 URL，屬既有頁面更新，會被近 48 小時變動掃描到，不需手動觸發）。

## 未採取行動的訊號（記錄理由，供明日參考）

- `uk-heatwave-red-warning-40c`：highImpZeroClick 最強訊號（見上），因超出 7 天時效窗口未處理。
- `appi-news-178`（中醫美顏針）：`highImpZeroClick`，「中醫美顏針」33 次曝光、排名 79.7、0 點擊。屬長青型科普可考慮的下一輪候選，本次因每日 3 檔額度優先給了訊號更集中（多查詢疊加同一批文章）的髖關節痛群組。
- `osteoarthritis-nonsurgical-prp`（wp-559）：已與新發布（2026-07-01）的 `knee-injection-ha-prp-steroid-comparison` 共用 `"PRP"`、`"退化性關節炎"` 兩個既有 tag，`relatedArticles()` 分數已足夠（同分類+2、同子分類+1、共用tag+2＝5），研判已有基本互相導流，本次不重複加碼。
