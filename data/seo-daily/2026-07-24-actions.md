# SEO 自動優化 2026-07-24

## ① 昨日賭注勝負

本 pipeline（`bin/seo-brain.sh`）自 07-09 之後到今天（07-24）中間有 15 天沒有留下 `-actions.md`（`git log --grep auto-claude-seo` 最後一筆真實改動仍是 07-08 的 revert），推測是排程曾暫停、近期才恢復，找不到更近的賭注可驗證。改用 07-09 當時記錄的既有賭注做健康檢查，對照今日 JSON（GSC range 2026-07-13..2026-07-20）：

- **極端高溫警報群組**（`extreme-heat-warning-guide`）：07-09 已判定為結構性非人類流量並永久 mute（`docs/lessons/high-impression-zero-click-bot-queries.md`）。今日 JSON 仍可見「嚴重高溫警報」143 次曝光、0 點擊、pos 7.7，「極端高溫警報」也是類似模式——與結案判讀一致，**不重新判讀、不採取行動**。
- **中醫美顏針群組**（`appi-news-178`，07-04 動過 description）：07-09 記錄「position 持續緩步改善」，今日「中醫美顏針」pos 81.68、「美顏針」pos 70.86，仍停留在很差的排名，看不出明顯持續改善的證據，但也沒有「連續 3 天以上明顯退步」的證據（沒有逐日數據可比對，15 天空窗期）。**不重複加碼**（見②的 gate 阻塞說明——這篇本來就無法再動）。
- **台電強韌電網文**（`taipower-grid-resilience-161kv-cable`，07-04 動過 tags）：今日「161kv」pos 4.86、impr 7、clicks 0，排名維持在頁一，訊號強度仍偏弱（個位數曝光），與 07-09「訊號強度不足」判讀一致，不重複加碼。
- **髖關節痛群組**（07-03 動過 tags）：今日仍可見多筆長尾查詢（`appi-news-91`「髖關節疼痛看哪一科」impr 10、pos 11.3 等），效果本來就要數週才顯現，且今天發現這整個 cluster 全部被 content gate 擋住（見②），無法再加碼。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-24.json`（GSC range 2026-07-13..2026-07-20，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：88 人；GSC 總覽：clicks 51、impressions 2731、position 12.82。
- `index.coverage`：11 個代表性頁面（首頁＋7 分類頁＋`/topics/`＋`/articles/`＋`/columns/`）皆 `Submitted and indexed`，無 Discovered/Crawled-not-indexed 訊號，這條路徑今天無事可做。
- 逐筆核對 `strikingDistance`（30 筆）與 `highImpZeroClick`（27 筆），排除已結案的高溫警報噪音群組後，找出的候選訊號與嘗試結果：
  1. **PCP/PRP 髖關節群組**（`hip-pcp-regenerative-therapy` / wp-432：「pcp prp差異」pos 10、impr 5，同時在 strikingDistance 與 highImpZeroClick）：判讀為最值得動手的訊號，槓桿＝幫 wp-432 補一個既有站內詞彙 tag `"PRP"`（`knee-injection-ha-prp-steroid-comparison` 已用過這個 tag），透過 `relatedArticles()` 的共享標籤加權把兩篇 PCP/PRP 比較文互相導流。**已寫入，但 `pnpm build` 的 `check-content.mjs` 擋下**：wp-432.md 本文裡有 4 處既有（非本次新增）AI 味 ERROR 命中（「不是X而是Y」×2、模糊引用×1）。因為 gate 是整檔全文重新掃描（不是只看 diff 的行），只要這篇檔案出現在變動清單裡就會被擋，而修正本文屬於「已發佈文章正文絕不可改動」的站規紅線（不是我能動的範圍）。**已 `git checkout` 撤回這個改動**。
  2. 順著同一條線，逐一測試了整個「髖關節痛看哪一科」cannibalization 候選群（`appi-news-91`／`wp-428`／`wp-426`，同主題新舊文互搶排名，本是 pageQueryCross 錯配的典型案例）：**三篇本文都各自有既存的 AI 味 ERROR**（模糊引用、「不是X而是Y」等，`wp-426` 甚至有 8 處），全部無法碰。
  3. 也測了 `appi-news-178`（中醫美顏針）、`claude-corps-nonprofit-talent-play`、`frontier-model-cyber-capability-governance`、`post-642`（海洋覆蓋率）：**全部本文已有既存 AI 味 ERROR，無法碰**。
  4. `taipower-grid-resilience-161kv-cable`（161kv，pos 4.86 但零點擊）：本文有 1 處既存「換句話說」ERROR，無法碰。
  5. 唯一測出**本文乾淨**、可以合法動手的候選是 `taiwan-offshore-wind-r3-3-admin-contract-update`（離岸風電 3-3，highImpZeroClick：impr 27、pos 23.07）。但依站規「訊號判讀與優化策略」：`publishDate` 2026-07-03、`contentType: news`，距今 21 天，已超出時效稿 7 天優化窗口，即使 highImpZeroClick 也不該花今天的額度。**判定不採用**。
- **本次判讀：今天沒有「訊號夠強＋本文乾淨＋在時效窗口內」三個條件同時成立的候選**——不是沒有訊號，是能動的候選全部撞到同一個結構性阻塞。

**結論：no-op**（內容面）。今天沒有任何 `src/` 檔案異動，只新增這篇留痕。

## ③ 今天做了什麼

無內容改動。僅新增本篇 `-actions.md`。

## 未採取行動的訊號（記錄理由，供明日參考）

- 高溫警報群組：結構性非人類流量，已於 07-09 永久 mute，不再判讀。
- `hip-pcp-regenerative-therapy`（wp-432，PRP tag 已測試過會通過 build 嗎？**不會**——本文既存 4 處 AI 味 ERROR 擋下，已撤回改動，明天起不要再嘗試同一招，除非有人先把本文的 AI 味清乾淨（那屬於編輯/內容產線職權，不是本 pipeline 授權範圍）。
- `appi-news-91`／`wp-428`／`wp-426`（髖關節痛看哪一科 cannibalization 群）：三篇本文皆有既存 AI 味 ERROR，本 pipeline 無法動任何 frontmatter（tags/description）欄位，因為 `check-content.mjs` 是整檔全文掃描、不分行。
- `appi-news-178`（中醫美顏針）：同上，本文已有既存 AI 味 ERROR。
- `claude-corps-nonprofit-talent-play`、`frontier-model-cyber-capability-governance`、`post-642`：同上，本文已有既存 AI 味 ERROR。
- `taipower-grid-resilience-161kv-cable`：本文有 1 處既存「換句話說」ERROR，且訊號強度（impr 7）本來就偏弱。
- `taiwan-offshore-wind-r3-3-admin-contract-update`：本文乾淨、可合法動手，但發佈已 21 天，超出時效稿 7 天優化窗口，依站規不列入今天行動。

## ⚠️ 給人類的觀察（結構性問題，非單日 no-op 原因）

今天完整測試了約 10 篇候選文章的 `check-content.mjs`（去 AI 味 gate），**多數命中的都是既存、非本次新增的違規**（模糊引用「研究顯示」、「不是X而是Y」下定義、破折號等）。這代表：只要這些文章的**本文**（不是我能碰的範圍）沒有人工清過，本 pipeline 之後每天都很可能繼續在同一批候選上撞牆、無法對 tags/description 這類允許動的欄位下手（因為 gate 是整檔全文重掃，不是只看改動的行）。這不是今天的緊急事項，但值得編輯部知道：**目前站上一批 2026-04～07 的健康類文章本文帶有既存 AI 味違規，擋住了 SEO 層對這些頁面補 tag/description 的空間**，若要讓這條 pipeline 未來能真正動到這些高訊號頁面，需要有人（編輯或內容產線）另案清理本文。
