# SEO 自動優化 2026-07-25

## ① 昨日賭注勝負

昨天（07-24）判讀結論是 no-op（所有測到的候選文章本文都已有既存 AI 味 ERROR，無法動 frontmatter），沒有留下可驗證的賭注。順帶回顧 07-24 記錄過的觀察：

- 高溫警報群組：結構性非人類流量，07-09 已永久 mute，今日不重新判讀。
- `taipower-grid-resilience-161kv-cable`（161kv）：07-24 記錄「本文有 1 處既存『換句話說』ERROR，無法碰」——今日重新逐一測試候選時再次確認同一結論（見下方②），維持不動。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-25.json`（GSC range 2026-07-14..2026-07-21，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：97 人；GSC 總覽：clicks 56、impressions 2877、position 12.48。
- `index.coverage`：11 個代表性頁面（首頁＋7 分類頁＋`/topics/`＋`/articles/`＋`/columns/`）皆 `Submitted and indexed`，這條路徑今天無事可做。
- 逐一測試 `strikingDistance`／`highImpZeroClick` 候選的本文是否過 `check-content.mjs`（因為 gate 是整檔全文掃描，只要檔案被列為變動檔就會重新掃全文，即使只改 frontmatter）：
  - **已擋（本文既存 AI 味 ERROR，無法碰）**：`post-642`（海洋覆蓋率，「不是X而是Y」）、`post-626`（離岸風電 3-3 ESG，拔高套語＋模糊引用）、`knee-injection-ha-prp-steroid-comparison`（PRP／玻尿酸比較，「換句話說」＋「值得一提的是」）、`liver-enzymes-got-gpt`（got 35，模糊引用＋「不是X而是Y」）、`amd-mext-memory-wall`（amd dram，「不是X而是Y」×2）、`benzo-pyrene-oil-long-term-exposure`（苯駢芘劑量，「不是X而是Y」×2）、`asic-over-gpu-taiwan-ic-design`（晶片，「不是X而是Y」）、`taiwan-health-data-why-now`（義隆電／國發基金，多處「不是X而是Y」「並非…而是」「真正的關鍵」）、`taiwan-health-data-platform-join`（ai 醫療，「不是X而是Y」）、`taipower-grid-resilience-161kv-cable`（161kv／台電161kv，「換句話說」，與昨日一致）。
  - **本文乾淨、可合法動手的候選**：`sk-hynix-packaging-hbm-tsmc`（hbm 台積電／sk 台積電，各 1 impr，訊號太弱）、`urban-canopy-climate-adaptation-network-launch`（國土計畫法，1 impr，訊號太弱；但同頁「水及流域永續發展行動計畫」已排名第 1 且有點擊，不需再動）、`patch-tuesday-ai-cve-surge`（cve-2026-47291，1 impr，訊號太弱）、`taiwan-water-consumption-fee-2026`（水費調漲2026，contentType news、發佈已 30 天，屬時效稿，依站規不列入）、**`medical-ai-compliance-lessons`（ai 醫療，highImpZeroClick 且同時在 strikingDistance：pos 12.5、4 impr、0 clicks；發佈 07-20，5 天內、在時效窗口內；文章型別 analysis／評論分析，屬長青向；GA4 近 7 天第 2 高瀏覽文章）**。
- **判定**：`medical-ai-compliance-lessons` 是今天唯一同時滿足「訊號夠強＋本文乾淨＋在時效窗口內」的候選，依「highImpZeroClick：改該頁 title/description/H1 更貼 query」策略動手。

## ③ 今天做了什麼

- **改動 1 個檔**：`src/content/articles/medical-ai-compliance-lessons.md`，只動 `description`（meta description）欄位，未動 `title`／內文／`tags`。
- **改法**：把原本埋在句中的「醫療 AI」「六個坑」關鍵詞移到描述開頭（原：「以醫療 AI 合規守門引擎 goalkeeper 為設計範例，拆解...六個工程與商業陷阱」→新：「醫療 AI 上線前最容易被低估的六個坑：...以醫療 AI 合規守門引擎 goalkeeper 為設計範例」），純粹重新排序既有事實，未新增/修改任何事實內容。
- **賭的訊號**：query「ai 醫療」、page `/articles/medical-ai-compliance-lessons/`，目前 pos 12.5、impr 4、clicks 0。預期：SERP 摘要更早出現查詢詞相關字眼，CTR 略升；因基期訊號量本來就小（4 impr/週），效果需要多週才看得出穩定趨勢。
- **技術細節**：commit `cb57ede`（[auto-claude-seo] 前綴），`git pull --rebase` 無衝突、`git push origin main` 成功。Gate 全綠：`pnpm install --frozen-lockfile`（lockfile up to date）→ `pnpm build`（exit 0，2350 頁、無錯誤）→ `pnpm check:links`（2599 HTML、140371 條內鏈，0 壞連結）。push 後未額外觸發部署/收錄（依站規由既有自動化接手）。

## 未採取行動的訊號（記錄理由，供明日參考）

- 高溫警報群組：結構性非人類流量，已永久 mute。
- `post-642`／`post-626`／`knee-injection-ha-prp-steroid-comparison`／`liver-enzymes-got-gpt`／`amd-mext-memory-wall`／`benzo-pyrene-oil-long-term-exposure`／`asic-over-gpu-taiwan-ic-design`／`taiwan-health-data-why-now`／`taiwan-health-data-platform-join`／`taipower-grid-resilience-161kv-cable`：本文皆有既存 AI 味 ERROR，本 pipeline 無法動任何 frontmatter 欄位（gate 整檔全文掃描）。這批文章持續累積，值得編輯部知道（見昨日已記錄的觀察，今天再次確認同一結構性阻塞，不重複記錄細節）。
- `sk-hynix-packaging-hbm-tsmc`／`urban-canopy-climate-adaptation-network-launch`／`patch-tuesday-ai-cve-surge`：本文乾淨可動，但訊號強度太弱（各僅 1 impression），今日不採取行動，留待訊號累積。
- `taiwan-water-consumption-fee-2026`：本文乾淨、strikingDistance 訊號尚可（水費調漲2026 pos 7.3、已有 1 click），但 `contentType: news`、發佈已 30 天，超出時效稿優化窗口，依站規不列入。
- `appi-news-91`／髖關節痛看哪一科 cannibalization 群（`wp-428`／`wp-426`）：與昨日相同，三篇本文皆有既存 AI 味 ERROR，無法動。
