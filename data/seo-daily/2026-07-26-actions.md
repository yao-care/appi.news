# SEO 自動優化 2026-07-26

## ① 昨日賭注勝負

07-25 賭注：`medical-ai-compliance-lessons`（query「ai 醫療」）改 description。

- 今日 GSC range 仍是 `2026-07-15..2026-07-22`——整段區間都在 07-25 改動之前，本來就看不到效果（GSC 有 2-3 天延遲，且改動要等下一版資料區間才會涵蓋）。
- 今日該 query/page 數字：pos 12.5、impr 4、clicks 0，與昨日基期完全一致，判定「還看不出來」，不撤回、繼續觀察。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-26.json`（GSC range 2026-07-15..2026-07-22，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：96 人；GSC 總覽：clicks 65、impressions 3151、position 12.12。
- `index.coverage`：11 個代表性頁面（首頁＋7 分類頁＋`/topics/`＋`/articles/`＋`/columns/`）皆 `Submitted and indexed`，這條路徑今天無事可做。
- `geo`（AEO/GEO 快篩）：`ok: true`、`gaps: []`，唯一 warn 是 sameAs 外部權威身分缺失——依站規/通用鐵則不可由程式捏造，非可行動項。
- 逐一測試 `strikingDistance`／`highImpZeroClick` 高訊號候選的本文能否過 `check-content.mjs`（gate 是整檔全文掃描，只改 frontmatter 也會重新掃全文）：
  - **`appi-news-178`**（中醫美顏針 34 impr／美顏針 19 impr／美顏針 中醫 7 impr／face美顏針 6 impr，同頁合計 66 impressions，今天訊號最強的候選）→ 本文有「並非…而是」ERROR（第 50 行，引用《黃帝內經》後的收束句），**擋**。
  - **`offshore-wind-r3-3-esg-scoring`**（= `post-626`，離岸風電 3-3，28 impr）→ 與 07-24/07-25 判讀一致，仍有既存 ERROR，**擋**（沿用前兩日結論，未重複列出細節）。
  - **`appi-news-91`／`wp-426`／`wp-428`／`wp-432`（髖關節痛看哪一科 cannibalization 群，含新測的 `appi-news-97`／`wp-432`）**：`appi-news-97`（張家誠醫師專訪）7 處 ERROR（「並非…而是」×1、「不是X而是Y」×3、「不只是…更是」×1、空泛收束×1）；`wp-432`（hip-pcp-regenerative-therapy，pcp prp差異 5 impr）4 處 ERROR（「不是X而是Y」×3、模糊引用「研究顯示」×1）。`appi-news-91`／`wp-426`／`wp-428` 沿用 07-24/07-25 已確認的 ERROR，**全數擋**。
  - **`post-684`**（= `pingtung-semiconductor-zone-groundbreaking`，屏東台積電預定地，3 impr）→ 本文有「不是X而是Y」ERROR（第 58 行結尾收束句），**擋**。
  - `taipower-grid-resilience-161kv-cable`／`knee-injection-ha-prp-steroid-comparison`／`post-642`／`liver-enzymes-got-gpt`／`amd-mext-memory-wall`／`benzo-pyrene-oil-long-term-exposure`／`asic-over-gpu-taiwan-ic-design`／`taiwan-health-data-why-now`／`taiwan-health-data-platform-join`：沿用 07-25 已逐檔確認的 ERROR，今日訊號未見顯著變化，**維持擋**、不重複測試。
  - **本文乾淨但訊號太弱（≤1 impression）**：`sk-hynix-packaging-hbm-tsmc`／`urban-canopy-climate-adaptation-network-launch`／`patch-tuesday-ai-cve-surge`（皆與 07-25 相同）。
  - **時效稿排除**（依站規「訊號判讀與優化策略」evergreen vs breaking 原則）：`taiwan-water-consumption-fee-2026`（水費調漲2026，發佈已 30+ 天）、`appi-news-391`（季軍賽是什麼，賽事新聞）、`police-good-deeds-2026-06-24`（薛琪樺，警政單日新聞，已超時效窗口）。
  - `fars-ai-research-validation-bottleneck`（fars ai，4 impr）已有 2 clicks、ctr 0.5、pos 6.5，本身表現良好，非待救訊號，不列入行動。
- **分類頁槓桿**：檢查 `src/pages/[category]/index.astro`，確認分類頁是純「發佈時間排序」自動列表（無可手動策展的相關文章/精選欄位），今天想拉的候選（`appi-news-178`／`wp-432`／`post-684`）發佈都久遠，不會出現在分類頁前 18 篇——要讓分類頁帶動這些舊文,需新增「精選/相關文章」UI 元件,屬中等風險的模板變更(影響全站 7 分類頁),超出單日保守執行的範圍,不在今天動手,留給有審閱的開發回合處理。

**判定**：今天沒有「訊號夠強＋本文乾淨」的候選，維持 no-op，不碰任何檔案。

## ③ 今天做了什麼

- 無內容改動。只 commit 本檔（留痕）。

## 未採取行動的訊號（記錄理由，供明日參考）

- `appi-news-178`／`appi-news-97`／`wp-432`／`post-684`：本文皆有既存 AI 味 ERROR，無法動任何 frontmatter 欄位（gate 整檔全文掃描）。`appi-news-178` 是今天訊號最強的候選（66 impressions），值得編輯部知道，若人工修掉那一句「並非…而是」，下一輪就能合法動 description/tags。
- 髖關節痛 cannibalization 群（`appi-news-91`／`appi-news-97`／`wp-426`／`wp-428`／`wp-432`）：五篇本文全數卡在既存 AI 味，是本站最大的一塊「有訊號但動不了」的區塊，已連續三天（07-24～07-26）記錄同一結構性阻塞。
- 分類頁精選欄位的想法：有實際槓桿（`appi-news-178`／`wp-432`／`post-684` 都太舊排不進分類頁前 18 篇），但需要新增 UI 元件、影響全站 7 分類頁，超出單日 ≤3 檔保守執行範圍，記錄供未來開發回合參考。
- `taiwan-water-consumption-fee-2026`／`appi-news-391`／`police-good-deeds-2026-06-24`：依站規時效稿排除原則，不列可行動。
