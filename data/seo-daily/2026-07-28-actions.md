# SEO 自動優化 2026-07-28

## ① 昨日賭注勝負

07-25 賭注：`medical-ai-compliance-lessons`（query「ai 醫療」）改 description。

- 今日 GSC range 為 `2026-07-17..2026-07-24`，仍是同一批延遲資料視窗（未滾動到 07-25 改動之後）。
- 今日該 query/page 數字：pos 12.5、impr 4、clicks 0，與 07-27 完全一致，判定「還看不出來」，不撤回、繼續觀察。
- 本文重跑 `check-content.mjs` 為乾淨（無 ERROR），確認先前改動沒有引入新問題。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-28.json`（GSC range 2026-07-17..2026-07-24，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：94 人；GSC 總覽：clicks 84、impressions 2991、position 11.88。
- `index.coverage`：11 個代表性頁面（首頁＋7 分類頁＋`/topics/`＋`/articles/`＋`/columns/`）皆 `Submitted and indexed`，這條路徑今天無事可做。
- `geo`（AEO/GEO 快篩）：`ok: true`、`gaps: []`，唯一 warn 仍是 sameAs 外部權威身分缺失，非可行動項。
- 逐一重測連續多日卡關的候選本文（`check-content.mjs`，整檔全文掃描）：
  - `appi-news-91`／`wp-428`（髖關節痛群）、`wp-432`（PCP）、`offshore-wind-r3-3-esg-scoring`、`appi-news-178`（美顏針群）、`taipower-grid-resilience-161kv-cable`、`claude-corps-nonprofit-talent-play`：**維持擋**，ERROR 位置與內容與 07-27 完全相同，代表本文仍未被人工修正。
  - **新測**`knee-injection-ha-prp-steroid-comparison`（膝關節注射比較，highImpZeroClick 3 個 query 合計約 13 impr）：「換句話說」＋「值得一提的是」兩處 ERROR，**擋**。
  - **新測**`liver-enzymes-got-gpt`（got/gpt 肝指數，4 impr）：模糊引用「專家認為」＋「不是X而是Y」，**擋**。
  - **`pingtung-semiconductor-zone-groundbreaking`／`post-684`（屏東台積電，3 impr，07-27 曾擋）：今日重測**已轉乾淨**（AI 味 ERROR 已被人工修掉），但 `publishDate` 為 2026-06-12，距今 46 天，屬單一動土典禮的時效稿，早已超出站規「highImpZeroClick 且發佈 7 天內」的可行動窗口，**列入可用但今日不動用**（留給日後若有後續報導再評估）。
  - **新測**`marine-heatwave-coverage-warning`／`post-642`（海洋熱浪覆蓋率，6 impr，pos 已達 2）：乾淨，但 GSC 命中的是舊網址 `articles/post-642/`（已 301 到正式網址，屬 Google 索引尚未跟上的正常延遲，非本層可處理），且訊號量極小（6 impr）、位置已經很好，非優化標的。
- **本日判定的可行動槓桿**：`髖關節痛/PCP/膝關節注射` 這一組長青型骨科／退化性關節文章（`hip-pain-which-specialist`、`hip-pcp-regenerative-therapy`、`knee-injection-ha-prp-steroid-comparison`）本文皆卡在既存 AI 味 ERROR、無法動 frontmatter，但**站內已有專題頁 `healthy-aging`（高齡健康與預防醫學）**，其宗旨（骨質疏鬆、肌少症、關節退化）與這組文章高度相關，且該專題頁**先前只手動收錄 2 篇同主題文章**（`osteoarthritis-nonsurgical-prp`、`hip-pain-symptoms-causes-treatment`），未涵蓋這批新文。這符合站規「內鏈：分類頁/相關文章模組/專題頁指向既有文章的連結」的明確授權範圍，且完全不觸碰任何文章本文/frontmatter，繞開了持續 5 天的 AI 味擋關問題。
  - 同時對照 `pageQueryCross`：`appi-news-91`（pos 10.4、impr 9）與 `hip-pain-which-specialist`（pos 8、impr 3）兩篇近乎重複的文章同時搶「髖關節疼痛看哪一科」這個 query；`wp-426`（既有站內文章）本身已在延伸閱讀連到 `hip-pain-which-specialist`，判定它才是站方認定的權威版本。今天的內鏈操作把權重導向 `hip-pain-which-specialist` 而非 `appi-news-91`，符合站規策略「查詢對到過期舊文——優先用內鏈把權重導去同主題最新、最權威的一篇」。

## ③ 今天做了什麼

- **改動 1 個檔案**：`src/content/topics/healthy-aging.md` 的 `articles` 陣列，新增 3 個 slug——`hip-pain-which-specialist`、`hip-pcp-regenerative-therapy`、`knee-injection-ha-prp-steroid-comparison`（原有 `osteoarthritis-nonsurgical-prp`、`hip-pain-symptoms-causes-treatment` 保留）。
- 賭的訊號／頁面：
  - `髖關節疼痛看哪一科`（`hip-pain-which-specialist`，目前 pos 8、impr 3）——賭專題頁內鏈能鞏固它相對 `appi-news-91` 的權威性，讓 Google 逐漸把排名資源導向這篇而非重複頁。
  - `pcp prp差異`（`hip-pcp-regenerative-therapy`，pos 10、impr 5）與 `knee-injection-ha-prp-steroid-comparison` 相關 3 個 query（`prp 玻尿酸 比較`／`玻尿酸 prp`／`膝蓋打玻尿酸費用`，合計約 13 impr）——賭專題頁聚合＋分類內鏈提升這幾篇的內部連結密度與主題關聯度，逐步墊高排名。
- 預期效果：`/topics/healthy-aging/` 頁面新增 3 張文章卡的內鏈入口；下次 GSC 資料視窗滾動後（約 4-11 天）觀察上述 query 的 position/impressions 是否改善。
- gate 全綠：`pnpm install --frozen-lockfile`（lockfile up to date）→ `pnpm build`（exit 0，含 check:design／check:content 前置關卡通過）→ `pnpm check:links`（2702 個 HTML、146650 條站內連結，無壞連結）。
- commit：`[auto-claude-seo] 2026-07-28: 高齡健康專題頁補收錄 3 篇髖關節/膝關節退化治療文章，內鏈導權重去重複頁`，`git pull --rebase origin main` 後 `git push origin main`。
- push 後依站規不需額外觸發部署/收錄推送，交給既有 deploy workflow 與 indexnow 自動處理。

## 未採取行動的訊號（記錄理由，供明日參考）

- **結構性阻塞持續第 5 天（07-24～07-28）**：`appi-news-91`／`wp-428`（髖關節痛群本文）、`wp-432`（PCP 本文）、`post-626`（離岸風電 3-3）、`appi-news-178`（美顏針群，全站最強候選 65+ impressions）、`taipower-grid-resilience-161kv-cable`、`claude-corps-nonprofit-talent-play`、`knee-injection-ha-prp-steroid-comparison`（新測）、`liver-enzymes-got-gpt`（新測）：本文皆有既存 AI 味 ERROR，本層無法動這些檔案本身的任何 frontmatter 欄位（gate 整檔全文掃描）。今日改用專題頁內鏈迂迴處理了其中 3 篇的內鏈曝光，但 description/tags 仍未解鎖。建議編輯部排時間修掉這批句型（多為「不是X而是Y」「換句話說」「值得一提的是」模糊引用），修完下一輪本層才能合法動這些文章自己的 description/tags。
- `pingtung-semiconductor-zone-groundbreaking`：AI 味已被人工修乾淨（好消息），但發佈已 46 天、屬單一動土典禮時效稿，超出 7 天可行動窗口，依站規不列今日可行動項。
- `marine-heatwave-coverage-warning`：乾淨但訊號量小（6 impr）、位置已達 2，且 GSC 命中的是已正確 301 的舊網址，非本層可處理範圍。
- `taiwan-water-consumption-fee-2026`／`appi-news-391`／`police-good-deeds-2026-06-24`：依站規時效稿排除原則，不列可行動。
- `medical-ai-compliance-lessons` 的 07-25 賭注持續觀察中，尚未到判讀窗口，數字連續 2 天持平。
