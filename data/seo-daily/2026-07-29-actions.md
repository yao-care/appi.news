# SEO 自動優化 2026-07-29

## ① 昨日賭注勝負

- **07-28 賭注**（`healthy-aging` 專題頁補收錄 3 篇髖關節/膝關節文章）：今日 GSC range 為 `2026-07-18..2026-07-25`，還在 07-28 改動之前的資料視窗，**還看不出來**，正常，預期 08-01 前後才會看到第一批訊號。
- **07-25 賭注**（`medical-ai-compliance-lessons`，query「ai 醫療」改 description）：今日 pos 12.5、impr 4、clicks 0，與 07-27、07-28 完全一致，已連續 3 天以上持平不變。非退步（無需回退），但持平已久，**判定「還看不出來」，繼續觀察，暫不追加動作**（避免同招連續加碼）。

## ② 今日判讀摘要

讀 `data/seo-daily/2026-07-29.json`（GSC range 2026-07-18..2026-07-25，GA4 近 7 天）：

- 台灣 Google 搜尋來的訪客：110 人（較前一輪回升）；GSC 總覽：clicks 84、impressions 2903、position 11.68。
- `index.coverage`：11 個代表性頁面（首頁＋7 分類頁＋`/topics/`＋`/articles/`＋`/columns/`）皆 `Submitted and indexed`，這條路徑今天無事可做。
- `geo`（AEO/GEO 快篩）：`ok: false`，唯一新 gap 是「AI 目標問句未納入追蹤（亞太新聞、專業觀點評論）」——這是 aeo-radar 觀察項，非本層 on-page 可處理範圍；warn 仍是 sameAs 外部權威身分缺失，非可行動項。
- 逐一重測 strikingDistance／highImpZeroClick 候選：
  - `appi-news-91`（髖關節痛群）、`appi-news-178`（美顏針群，仍是全站最強候選，65+ impressions）、`taipower-grid-resilience-161kv-cable`、`claude-corps-nonprofit-talent-play`、`knee-injection-ha-prp-steroid-comparison`、`liver-enzymes-got-gpt`、`taiwan-health-data-why-now`（新測，「並非…而是」「不是X而是Y」「真正的關鍵」共 4 處）：**維持擋**，`check-content.mjs` 命中既存 AI 味 ERROR，本層無法動這些檔案的任何 frontmatter。
  - **新測乾淨**：`ldl-cholesterol-targets`（LDL 膽固醇指南，strikingDistance「ldl 135」pos 12 impr 2）、`taiwan-reservoir-dredging-2026`（水庫清淤，strikingDistance「台積電用水」pos 8 impr 2）。後者是 07-25 發佈的單一時效性政策新聞稿（4 天內、但訊號量僅 1-2 impr，非 highImpZeroClick），依站規「時效稿邊際效益低」不列可行動；前者是長青健檢指南（無時效性），列為今日可行動標的。
  - 查證 `fars-ai-research-validation-bottleneck`（GSC 仍有殘留訊號，query「fars ai」pos 6 impr 3）：該文章已於先前一次「剪除未上線科技新聞排程存量」的批次中被刪除（git log 顯示已不存在於 repo），非本層可處理，僅是 GSC 索引延遲殘留，記錄不再重複查。
- **意外發現並處理**：`pnpm build` gate 失敗，追查後定位到與本層無關的既存 bug——`src/content/articles/appi-news-453.md`（今早 02:21 由其他產線／人工提交的羽球新聞）標籤 `["周天成","羽球","奪冠","超級1000","年長紀錄","中國羽球公開賽","台灣羽球"]` 全數不在 `src/config/tags.ts` 的受控詞彙表內，導致 Astro content collection schema 驗證擋下整個 build——這代表自 07-29 02:21 起，main 分支的每一次 build（含 deploy.yml 的排程重建）理論上都會失敗。因屬於「tags」欄位、且是資料完整性錯誤（非風格選擇），在本層授權範圍內單獨修正為既有詞彙表內語意相近的 `["國際賽事", "中國"]`，未動標題/內文/其他欄位，獨立成一個 commit 以利單獨 revert。

## ③ 今天做了什麼

**改動 2 個檔案，分 2 個 commit：**

1. **`src/content/articles/appi-news-453.md`**（bug fix，非 SEO 賭注）：標籤改為 `["國際賽事", "中國"]`，修正阻擋全站 build 的既存 schema 違規。commit `9b4e1af`。
2. **`src/content/articles/ldl-cholesterol-targets.md`**（SEO 賭注）：description 補入「135」（PCSK9 健保給付起始值由 135 下修到 100，本文既有事實，非新增杜撰）並提前點名「參考值 130」。commit `e12a772`。
   - 賭的訊號：`ldl 135`（pos 12、impr 2，strikingDistance）；連帶覆蓋 `ldl 膽固醇`（pos 80、impr 1）、`壞膽固醇指數`（pos 75、impr 1）等同頁其他長尾詞。
   - 預期效果：meta description 更貼近搜尋詞用字，下次 GSC 資料視窗滾動後（約 4-11 天，即 08-02～08-09）觀察 position/impressions 是否改善。

- gate 全綠：`pnpm install --frozen-lockfile`（lockfile up to date）→ `pnpm build`（修正 appi-news-453 後 exit 0，含 check:design／check:content／check:tags 前置關卡通過）→ `pnpm check:links`（1052 個 HTML、66452 條站內連結，無壞連結）。
- `git pull --rebase origin main` 無衝突（fast-forward）後 `git push origin main`，兩個 commit 依序推送成功。
- push 後依站規不需額外觸發部署/收錄推送，交給既有 deploy workflow 與 indexnow 自動處理。

## 未採取行動的訊號（記錄理由，供明日參考）

- **結構性阻塞持續第 6 天（07-24～07-29）**：`appi-news-91`／`wp-428`（髖關節痛群本文）、`wp-432`（PCP）、`appi-news-178`（美顏針群，全站最強候選）、`taipower-grid-resilience-161kv-cable`、`claude-corps-nonprofit-talent-play`、`knee-injection-ha-prp-steroid-comparison`、`liver-enzymes-got-gpt`、`taiwan-health-data-why-now`（新測，本輪新增）：本文皆有既存 AI 味 ERROR，本層無法動這些檔案本身的任何 frontmatter 欄位。`appi-news-178`（美顏針，中醫分類，subcategory tcm-integrative）目前站上無對應主題可做內鏈迂迴（現有 11 個 topics 皆非中醫/美容主題），建議編輯部優先修掉這篇的 AI 味句型（多為模糊引用／換句話說類），解鎖後本層才能處理其 description/tags，且量體最大（65+ impressions）。
- `taiwan-reservoir-dredging-2026`：內容乾淨可動，但屬 07-25 發佈的單一政策新聞稿（4 天內）、訊號量僅 1-2 impr，非 highImpZeroClick，依站規時效稿邊際效益低原則不列今日可行動項。
- `medical-ai-compliance-lessons` 的 07-25 賭注已連續 3+ 天持平（pos 12.5、impr 4、clicks 0），繼續觀察、暫不重複加碼同一頁。
- `fars-ai-research-validation-bottleneck`：GSC 仍有殘留訊號（3 impr），但文章本體已在先前批次中被刪除，非本層可處理，純索引延遲殘留。
- **審查建議（超出本次交付範圍，僅記錄）**：`appi-news-453.md` 的標籤違規為何未被上游產線的 `check-tags.mjs` 自檢擋下（該腳本單獨對此檔執行時能正確攔截），值得追查是哪條產線／流程繞過了自檢就直接提交，避免同類 build-breaking 內容再次發生。
