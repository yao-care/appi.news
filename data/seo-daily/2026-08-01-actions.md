# SEO 自動優化 2026-08-01

## ① 昨日賭注勝負

昨日（07-31 actions.md）賭注：`gongguan-mei-jiang-korean-bbq-closing` description 補「梅江韓國銅盤烤肉」公館店 + 最後營業日。**今天無法查核**——GSC API 對 `sc-domain:appi.news` 回傳權限錯誤（見下），position/impressions/clicks 全部讀不到。首訊窗預期 08-04～08-11，屆時若權限已修復再補查。

## ② 今日判讀摘要 —— GSC 存取權限中斷，非資料延遲

今天 `data/seo-daily/2026-08-01.json` 的 `gsc`／`index`／`targetTrend` 三段全部回傳：

```
User does not have sufficient permission for site 'sc-domain:appi.news'.
```

**已排除「暫時性」可能**：現場重跑 `node /root/seo-ops/bin/identity-audit.mjs --site appi.news` 直接對 Search Console API 查證，服務帳號 `ga4-insights@yaocare.iam.gserviceaccount.com` 目前**完全看不到 `sc-domain:appi.news`**（連在 GSC 站台清單裡都不存在），但同一把金鑰能看到其餘 10 個站（folk.tw／sutta.io／twdro.net／vuko.life／evidencetoday.news／dreamer868.com／olderkkk.com／arthurs.tw／yao.care／weiqi.kids，多數還是 `siteOwner`）。GA4（`541946427`）不受影響，今天照常有數據。

對照 `/root/seo-ops/notes/identity-migration.md` 的遷移追蹤表：appi.news 那列（第 9 站）八個欄位全部還是 `⏳`（未開始遷移）——代表這**不是**刻意的權限收斂/隔離動作的預期結果，比較像是在處理其他站台時的**意外副作用**（或帳號端被手動動過），需要人工判斷、本層無法自行修復（加回 Search Console 使用者屬 Google 帳號層級操作，不在本層 repo 授權範圍內）。

**影響範圍**：`strikingDistance`／`highImpZeroClick`／`pageQueryCross`／`index.coverage` 四項訊號來源今天全部不可用 → 站規策略清單中依賴這些訊號的判讀全部無法進行。GA4 的 `topPages` 有資料，但站規明確要求分類頁/文章優化要以 GSC 查詢級訊號（strikingDistance/highImpZeroClick/pageQueryCross）為依據，光靠 GA4 頁面瀏覽量猜 description/內鏈改動屬於「無憑無據亂改」，今天不執行。

`geo`：`ok: false`，`targetQ` 缺口（AI 目標問句追蹤，屬 aeo-radar 職權）、`sameAs` warn（站外權威身分）——與昨日相同，本層無可行動項。

## ③ 今天做了什麼

**無內容改動（no-op）**。原因：核心優化訊號來源（GSC）今天對本站無存取權限，不是「沒有可行動訊號」而是「拿不到訊號」，寧可不動也不要憑 GA4 頁面瀏覽量猜測改動。

- 只 commit 本檔（`data/seo-daily/2026-08-01-actions.md`），不涉及 `src/` 任何檔案，跳過 gate（無改動可驗）。
- `git pull --rebase origin main` → `git push origin main`。

## 需要人工處理的事項

- **服務帳號 `ga4-insights@yaocare.iam.gserviceaccount.com` 需要被重新加回 `sc-domain:appi.news` 的 Search Console 使用者清單**（建議至少 Full User，若本站有用到 Indexing API 需 Owner）。這需要有 appi.news GSC 屬性擁有權限的 Google 帳號登入 Search Console → 設定 → 使用者和權限 → 新增使用者 → 貼上該服務帳號 email。
- 在修復前，本層每天的 GSC 訊號判讀都會持續失效（等同今天），weekly-report／aeo-radar 等其他消費 GSC 的自動化可能也受影響，建議儘快處理。
- 修復後下一次跑本 pipeline 會自動恢復正常判讀，不需額外操作。
