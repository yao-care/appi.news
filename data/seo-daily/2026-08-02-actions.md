# SEO 自動優化 2026-08-02

## ① 昨日賭注勝負

昨日（08-01）同樣是 no-op（GSC 權限中斷），無新賭注可查核。往前一天（07-31）的賭注（`gongguan-mei-jiang-korean-bbq-closing` description 補充）今天依然無法查核——GSC 權限仍未恢復，position/impressions/clicks 全部讀不到。首訊窗（08-04～08-11）尚未到期，繼續等權限修復。

## ② 今日判讀摘要 —— GSC 存取權限中斷第 2 天，非資料延遲

`data/seo-daily/2026-08-02.json` 的 `gsc`／`index`／`targetTrend` 三段今天**依然**回傳：

```
User does not have sufficient permission for site 'sc-domain:appi.news'.
```

現場重跑 `node /root/seo-ops/bin/identity-audit.mjs --site appi.news` 再次確認：服務帳號 `ga4-insights@yaocare.iam.gserviceaccount.com` 仍完全看不到 `sc-domain:appi.news`（GSC 站台清單裡不存在該資源），與昨天（08-01）狀態一致，**尚未修復**。GA4（`541946427`）不受影響，今天照常有數據（sessions 371、taiwanOrganicSessions 208，7 日視窗）。

**影響範圍不變**：`strikingDistance`／`highImpZeroClick`／`pageQueryCross`／`index.coverage` 四項訊號來源連續第 2 天不可用。站規要求分類頁/文章優化以 GSC 查詢級訊號為依據，光靠 GA4 頁面瀏覽量猜 description/內鏈改動屬無憑無據，今天繼續不執行。

`geo`：`ok: false`，`targetQ` 缺口（AI 目標問句追蹤，屬 aeo-radar 職權，非本層授權範圍）——與前兩日相同，本層無可行動項。

## ③ 今天做了什麼

**無內容改動（no-op）**，原因與昨天相同：核心優化訊號來源（GSC）連續第 2 天對本站無存取權限，寧可不動也不要憑 GA4 頁面瀏覽量猜測改動。

- 只 commit 本檔（`data/seo-daily/2026-08-02-actions.md`），不涉及 `src/` 任何檔案，跳過 gate（無改動可驗）。
- `git pull --rebase origin main` → `git push origin main`。

## 需要人工處理的事項（與昨天相同，尚未解決）

- **服務帳號 `ga4-insights@yaocare.iam.gserviceaccount.com` 需要被重新加回 `sc-domain:appi.news` 的 Search Console 使用者清單**（建議至少 Full User，若本站有用到 Indexing API 需 Owner）。需要有 appi.news GSC 屬性擁有權限的 Google 帳號登入 Search Console → 設定 → 使用者和權限 → 新增使用者 → 貼上該服務帳號 email。
- 已連續 2 天無法判讀，weekly-report／aeo-radar 等其他消費 GSC 的自動化可能也持續受影響，建議儘快處理。
- 修復後下一次跑本 pipeline 會自動恢復正常判讀，不需額外操作。
