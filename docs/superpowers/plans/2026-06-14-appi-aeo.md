# APPI News AEO / GEO 強化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 補齊本站 AEO/GEO 缺口，讓 LLM 搜尋更容易正確理解並引用本站：新增 llms.txt / llms-full.txt、robots 明確放行 AI 爬蟲、強化 JSON-LD 與 SEO meta、RSS 全文+作者、作者頁 UX、newsroom 連動。

**Architecture:** 沿用既有 Astro endpoint 模式（`robots.txt.ts`、`rss.xml.ts`）與既有 SEO 模組（`jsonld.ts`、`SEOHead.astro`、`BaseLayout.astro`）。可測邏輯抽成純函式（`src/utils/llms.ts`、`jsonld.ts`）走 vitest TDD；頁面與 endpoint 做組裝，以 `pnpm build` + 產物檢查驗收。不改 schema、不重構。

**Tech Stack:** Astro 5（static）、TypeScript、vitest、@astrojs/rss、@astrojs/sitemap（既有）、markdown-it + sanitize-html（RSS 全文，新增）。

**已驗證的現況（實作依據）：**
- `SITE`（`src/config/site.ts`）：`name`、`tagline`、`taglineEn`、`description`、`lang`、`locale`、`email`、`defaultOgImage`、`org`。無 Twitter/X 帳號欄位。
- 分類：`src/config/categories.ts` 的 `CATEGORIES`（`slug`/`name`/`description`/`subcategories`）。
- 內容 helper（`src/utils/content.ts`）：`getPublishedArticles()`、`getAuthors()`、`getAuthorMap()`、`getColumns()`、`getTopics()`、`articleSlug()`、`coverImageFor()`、`authorAvatar()`、`authorHasPage()`、`byAuthor()`。
- 網址（`src/utils/url.ts`）：`url()`、`absoluteUrl(path, site)`、`asset()`。
- `jsonld.ts` 的 `articleLd()` 目前用 `authorName`/`authorPath`，author 只有 name+url。
- `SEOHead.astro` props：title/description/path/ogImage/ogType/noindex/publishedTime/modifiedTime。由 `BaseLayout.astro` 轉傳。
- `rss.xml.ts`：items 含 title/description/pubDate/link/categories。
- vitest 設定已含 `src/**/*.test.ts`（純函式可直接放 src 旁）。

---

## File Structure

**新增：**
- `src/utils/llms.ts` — 純函式 `buildLlmsTxt()`、`buildLlmsFullTxt()`（組 llms 文字，輸入為已攤平的 plain 資料）。
- `src/utils/llms.test.ts` — 上述純函式測試。
- `src/pages/llms.txt.ts` — endpoint，取資料 + 解析網址 + 呼叫 `buildLlmsTxt()`。
- `src/pages/llms-full.txt.ts` — endpoint，呼叫 `buildLlmsFullTxt()`。
- `src/utils/jsonld.test.ts` — `articleLd()` 強化後的測試。

**修改：**
- `src/pages/robots.txt.ts` — 明確列 AI 爬蟲。
- `src/utils/jsonld.ts` — `articleLd()` 改用完整 Person author + keywords + about。
- `src/pages/articles/[slug].astro` — 更新 `articleLd()` 呼叫；傳 author/keywords 給 BaseLayout。
- `src/components/seo/SEOHead.astro` — 新增 author/article:author/keywords/twitter meta。
- `src/layouts/BaseLayout.astro` — 轉傳 authorName/authorPath/keywords。
- `src/config/site.ts` — 新增可選 `x`（Twitter/X handle，預設空）。
- `src/pages/rss.xml.ts` — 每篇加 author + 全文 content。
- `src/pages/authors/[slug].astro` — 顯示 website/email/joinedDate。
- `.claude/skills/newsroom/SKILL.md` — tags 與 FAQ 連動一句。

---

## Task 1: llms 文字產生純函式 + 測試

**Files:**
- Create: `src/utils/llms.ts`
- Test: `src/utils/llms.test.ts`

- [ ] **Step 1: 先寫失敗測試** — `src/utils/llms.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildLlmsTxt, buildLlmsFullTxt } from './llms.ts';

const base = {
  name: 'APPI News',
  tagline: '亞太專業觀點',
  description: 'APPI News｜亞太專業觀點…',
  homeUrl: 'https://yao-care.github.io/appi.news/',
  fullTxtUrl: 'https://yao-care.github.io/appi.news/llms-full.txt',
  sitemapUrl: 'https://yao-care.github.io/appi.news/sitemap-index.xml',
  rssUrl: 'https://yao-care.github.io/appi.news/rss.xml',
  authors: [
    { name: 'CΛ / Lightman', title: '科技評論', url: 'https://yao-care.github.io/appi.news/authors/lightman/', specialties: ['醫療 AI'] },
  ],
  categories: [
    { name: '科技', description: '追蹤 AI…', url: 'https://yao-care.github.io/appi.news/tech/' },
  ],
  articles: [
    { title: '測試文章', url: 'https://yao-care.github.io/appi.news/articles/post-1/', description: '一句描述' },
  ],
};

describe('buildLlmsTxt', () => {
  it('含品牌、作者、分類、文章與索引連結', () => {
    const out = buildLlmsTxt(base);
    expect(out).toContain('# APPI News');
    expect(out).toContain('亞太專業觀點');
    expect(out).toContain('CΛ / Lightman');
    expect(out).toContain('科技');
    expect(out).toContain('測試文章');
    expect(out).toContain('https://yao-care.github.io/appi.news/articles/post-1/');
    expect(out).toContain('/llms-full.txt');
    expect(out).toContain('sitemap-index.xml');
    expect(out).toContain('rss.xml');
    expect(out).toContain('引用'); // 含引用指引
  });
});

describe('buildLlmsFullTxt', () => {
  it('每篇含標題、網址、日期、描述與重點', () => {
    const out = buildLlmsFullTxt({
      name: 'APPI News',
      homeUrl: 'https://yao-care.github.io/appi.news/',
      articles: [
        { title: '測試文章', url: 'https://x/articles/post-1/', date: '2026-06-14', category: '科技', description: '一句描述', highlights: ['重點一', '重點二'] },
      ],
    });
    expect(out).toContain('測試文章');
    expect(out).toContain('https://x/articles/post-1/');
    expect(out).toContain('2026-06-14');
    expect(out).toContain('一句描述');
    expect(out).toContain('重點一');
    expect(out).toContain('重點二');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗** — `pnpm test llms`。Expected: FAIL（找不到 `./llms.ts`）。

- [ ] **Step 3: 寫實作** — `src/utils/llms.ts`:
```ts
export interface LlmsAuthor {
  name: string;
  title?: string;
  url?: string;
  specialties: string[];
}
export interface LlmsCategory {
  name: string;
  description: string;
  url: string;
}
export interface LlmsArticle {
  title: string;
  url: string;
  description: string;
}

export function buildLlmsTxt(d: {
  name: string;
  tagline: string;
  description: string;
  homeUrl: string;
  fullTxtUrl: string;
  sitemapUrl: string;
  rssUrl: string;
  authors: LlmsAuthor[];
  categories: LlmsCategory[];
  articles: LlmsArticle[];
}): string {
  const lines: string[] = [];
  lines.push(`# ${d.name}｜${d.tagline}`, '');
  lines.push(`> ${d.description}`, '');
  lines.push('## 關於', '');
  lines.push(`${d.name} 是繁體中文的專業觀點平台，結合 AI 輔助寫作、專家審稿與媒體刊登。網站：${d.homeUrl}`, '');
  if (d.authors.length) {
    lines.push('## 作者', '');
    for (const a of d.authors) {
      const title = a.title ? `（${a.title}）` : '';
      const spec = a.specialties.length ? `專長：${a.specialties.join('、')}。` : '';
      const link = a.url ? ` ${a.url}` : '';
      lines.push(`- ${a.name}${title}：${spec}${link}`.trimEnd());
    }
    lines.push('');
  }
  if (d.categories.length) {
    lines.push('## 主題', '');
    for (const c of d.categories) {
      lines.push(`- [${c.name}](${c.url})：${c.description}`);
    }
    lines.push('');
  }
  if (d.articles.length) {
    lines.push('## 重點文章', '');
    for (const a of d.articles) {
      lines.push(`- [${a.title}](${a.url})：${a.description}`);
    }
    lines.push('');
  }
  lines.push('## 引用指引', '');
  lines.push(`歡迎引用本站內容，請標注來源「${d.name}」與作者名並連結原文。`, '');
  lines.push('## 索引', '');
  lines.push(`- 完整文章索引：${d.fullTxtUrl}`);
  lines.push(`- Sitemap：${d.sitemapUrl}`);
  lines.push(`- RSS：${d.rssUrl}`);
  lines.push('');
  return lines.join('\n');
}

export function buildLlmsFullTxt(d: {
  name: string;
  homeUrl: string;
  articles: {
    title: string;
    url: string;
    date: string;
    category: string;
    description: string;
    highlights: string[];
  }[];
}): string {
  const lines: string[] = [];
  lines.push(`# ${d.name}｜完整文章索引`, '');
  lines.push(`網站：${d.homeUrl}`, '');
  for (const a of d.articles) {
    lines.push(`## ${a.title}`);
    lines.push(`- 網址：${a.url}`);
    lines.push(`- 日期：${a.date}`);
    lines.push(`- 分類：${a.category}`);
    lines.push(`- 描述：${a.description}`);
    if (a.highlights.length) {
      lines.push('- 重點：');
      for (const h of a.highlights) lines.push(`  - ${h}`);
    }
    lines.push('');
  }
  return lines.join('\n');
}
```

- [ ] **Step 4: 跑測試確認通過** — `pnpm test llms`。Expected: PASS。

- [ ] **Step 5: Commit:**
```bash
git add src/utils/llms.ts src/utils/llms.test.ts
git commit -m "feat(aeo): llms.txt 文字產生純函式 + 測試"
```

---

## Task 2: llms.txt / llms-full.txt endpoints

**Files:**
- Create: `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`

- [ ] **Step 1: 寫 `src/pages/llms.txt.ts`:**
```ts
import type { APIContext } from 'astro';
import { SITE } from '@/config/site';
import { CATEGORIES } from '@/config/categories';
import {
  getPublishedArticles,
  getAuthors,
  authorHasPage,
  articleSlug,
} from '@/utils/content';
import { absoluteUrl } from '@/utils/url';
import { buildLlmsTxt } from '@/utils/llms';

export async function GET(context: APIContext) {
  const site = context.site;
  const articles = await getPublishedArticles();
  const authors = (await getAuthors()).filter((a) => a.data.active && authorHasPage(a));

  const body = buildLlmsTxt({
    name: SITE.name,
    tagline: SITE.tagline,
    description: SITE.description,
    homeUrl: absoluteUrl('/', site),
    fullTxtUrl: absoluteUrl('/llms-full.txt', site),
    sitemapUrl: absoluteUrl('/sitemap-index.xml', site),
    rssUrl: absoluteUrl('/rss.xml', site),
    authors: authors.map((a) => ({
      name: a.data.name,
      title: a.data.displayTitle,
      url: absoluteUrl(`/authors/${a.id}/`, site),
      specialties: a.data.specialties,
    })),
    categories: CATEGORIES.map((c) => ({
      name: c.name,
      description: c.description,
      url: absoluteUrl(`/${c.slug}/`, site),
    })),
    articles: articles.slice(0, 20).map((a) => ({
      title: a.data.title,
      url: absoluteUrl(`/articles/${articleSlug(a)}/`, site),
      description: a.data.description,
    })),
  });

  return new Response(body, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8' },
  });
}
```

- [ ] **Step 2: 寫 `src/pages/llms-full.txt.ts`:**
```ts
import type { APIContext } from 'astro';
import { SITE } from '@/config/site';
import { getPublishedArticles, articleSlug } from '@/utils/content';
import { getCategoryName } from '@/config/categories';
import { absoluteUrl } from '@/utils/url';
import { isoDate } from '@/utils/date';
import { buildLlmsFullTxt } from '@/utils/llms';

export async function GET(context: APIContext) {
  const site = context.site;
  const articles = await getPublishedArticles();
  const body = buildLlmsFullTxt({
    name: SITE.name,
    homeUrl: absoluteUrl('/', site),
    articles: articles.map((a) => ({
      title: a.data.title,
      url: absoluteUrl(`/articles/${articleSlug(a)}/`, site),
      date: isoDate(a.data.publishDate).slice(0, 10),
      category: getCategoryName(a.data.category),
      description: a.data.description,
      highlights: a.data.highlights,
    })),
  });
  return new Response(body, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8' },
  });
}
```
（`isoDate` 來自 `@/utils/date`，已在文章頁使用，確認其匯出存在；若簽章不同，改用其回傳值前 10 碼取日期。）

- [ ] **Step 3: build 驗證** — `pnpm build`。Expected: 成功，且 `dist/llms.txt`、`dist/llms-full.txt` 產生。檢查：
```bash
test -f dist/llms.txt && test -f dist/llms-full.txt && echo OK
grep -q "# APPI News" dist/llms.txt && grep -q "/articles/" dist/llms.txt && echo llms-ok
grep -q "完整文章索引" dist/llms-full.txt && echo full-ok
```
Expected: 印出 `OK`、`llms-ok`、`full-ok`。

- [ ] **Step 4: Commit:**
```bash
git add src/pages/llms.txt.ts src/pages/llms-full.txt.ts
git commit -m "feat(aeo): llms.txt / llms-full.txt endpoints"
```

---

## Task 3: robots.txt 明確放行 AI 爬蟲

**Files:**
- Modify: `src/pages/robots.txt.ts`

- [ ] **Step 1: 改 body 內容** — 把現有 `const body = ...` 區塊替換為：
```ts
  const aiBots = [
    'GPTBot',
    'OAI-SearchBot',
    'ChatGPT-User',
    'ClaudeBot',
    'Claude-Web',
    'anthropic-ai',
    'PerplexityBot',
    'Google-Extended',
    'CCBot',
    'Bytespider',
    'Amazonbot',
  ];
  const aiBlock = aiBots
    .map((b) => `User-agent: ${b}`)
    .join('\n') + '\nAllow: /\nDisallow: /admin/\n';
  const body = `${aiBlock}
User-agent: *
Allow: /
Disallow: /admin/

Sitemap: ${sitemapUrl}
`;
```
（保留檔案上方既有的 `sitemapUrl` 計算與 `return new Response(...)` 不動。）

- [ ] **Step 2: build 驗證** — `pnpm build` 後：
```bash
grep -q "User-agent: GPTBot" dist/robots.txt && grep -q "User-agent: ClaudeBot" dist/robots.txt && grep -q "User-agent: PerplexityBot" dist/robots.txt && echo ai-ok
grep -q "User-agent: \*" dist/robots.txt && grep -q "Disallow: /admin/" dist/robots.txt && grep -q "Sitemap:" dist/robots.txt && echo base-ok
```
Expected: `ai-ok`、`base-ok`。

- [ ] **Step 3: Commit:**
```bash
git add src/pages/robots.txt.ts
git commit -m "feat(aeo): robots.txt 明確放行 AI 爬蟲"
```

---

## Task 4: JSON-LD 強化（完整 Person author + keywords + about）

**Files:**
- Modify: `src/utils/jsonld.ts`、`src/pages/articles/[slug].astro`
- Test: `src/utils/jsonld.test.ts`

- [ ] **Step 1: 先寫失敗測試** — `src/utils/jsonld.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { articleLd } from './jsonld.ts';

const site = 'https://yao-care.github.io/appi.news/';
const baseArgs = {
  headline: '標題',
  description: '描述',
  path: '/articles/post-1/',
  image: 'https://x/cover.webp',
  datePublished: '2026-06-14T09:00:00+08:00',
  dateModified: '2026-06-14T09:00:00+08:00',
  author: {
    name: 'CΛ / Lightman',
    path: '/authors/lightman/',
    image: 'https://x/lightman.jpg',
    jobTitle: '科技評論',
    sameAs: ['https://weiqi.kids/'],
  },
  section: '科技',
  keywords: ['LLM', '醫療AI'],
  about: ['數位健康'],
};

describe('articleLd', () => {
  it('author 展開為完整 Person（含 image/jobTitle/sameAs/url）', () => {
    const ld = articleLd(site, baseArgs) as any;
    expect(ld.author['@type']).toBe('Person');
    expect(ld.author.name).toBe('CΛ / Lightman');
    expect(ld.author.image).toBe('https://x/lightman.jpg');
    expect(ld.author.jobTitle).toBe('科技評論');
    expect(ld.author.sameAs).toContain('https://weiqi.kids/');
    expect(ld.author.url).toContain('/authors/lightman/');
  });
  it('keywords 與 about 輸出', () => {
    const ld = articleLd(site, baseArgs) as any;
    expect(ld.keywords).toContain('LLM');
    expect(JSON.stringify(ld.about)).toContain('數位健康');
  });
  it('isNews 時型別為 NewsArticle', () => {
    const ld = articleLd(site, { ...baseArgs, isNews: true }) as any;
    expect(ld['@type']).toBe('NewsArticle');
  });
  it('author 無 path/image 時仍合法（只有 name）', () => {
    const ld = articleLd(site, { ...baseArgs, author: { name: '編輯部' } }) as any;
    expect(ld.author['@type']).toBe('Person');
    expect(ld.author.name).toBe('編輯部');
    expect(ld.author.url).toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑測試確認失敗** — `pnpm test jsonld`。Expected: FAIL（目前 `articleLd` 用 `authorName`，新測試傳 `author` 物件、讀 keywords/about）。

- [ ] **Step 3: 改 `articleLd()`** — 把 `src/utils/jsonld.ts` 的 `articleLd` 整個函式替換為：
```ts
export function articleLd(
  site: SiteUrl,
  a: {
    headline: string;
    description: string;
    path: string;
    image: string;
    datePublished: string;
    dateModified: string;
    author: {
      name: string;
      path?: string;
      image?: string;
      jobTitle?: string;
      sameAs?: string[];
    };
    section?: string;
    isNews?: boolean;
    keywords?: string[];
    about?: string[];
  },
) {
  return {
    '@context': 'https://schema.org',
    '@type': a.isNews ? 'NewsArticle' : 'Article',
    headline: a.headline,
    description: a.description,
    image: [a.image],
    datePublished: a.datePublished,
    dateModified: a.dateModified || a.datePublished,
    inLanguage: SITE.lang,
    ...(a.section ? { articleSection: a.section } : {}),
    ...(a.keywords && a.keywords.length ? { keywords: a.keywords.join(', ') } : {}),
    ...(a.about && a.about.length
      ? { about: a.about.map((name) => ({ '@type': 'Thing', name })) }
      : {}),
    mainEntityOfPage: {
      '@type': 'WebPage',
      '@id': absoluteUrl(a.path, site),
    },
    author: {
      '@type': 'Person',
      name: a.author.name,
      ...(a.author.path ? { url: absoluteUrl(a.author.path, site) } : {}),
      ...(a.author.image ? { image: a.author.image } : {}),
      ...(a.author.jobTitle ? { jobTitle: a.author.jobTitle } : {}),
      ...(a.author.sameAs && a.author.sameAs.length ? { sameAs: a.author.sameAs } : {}),
    },
    publisher: {
      '@type': 'Organization',
      name: SITE.name,
      logo: {
        '@type': 'ImageObject',
        url: absoluteUrl(SITE.defaultOgImage, site),
      },
    },
  };
}
```

- [ ] **Step 4: 跑測試確認通過** — `pnpm test jsonld`。Expected: PASS。

- [ ] **Step 5: 更新呼叫端** — 在 `src/pages/articles/[slug].astro`：
  - 於 import 區把 `getTopics` 加入 `@/utils/content` 的具名匯入，並 import `authorAvatar`。
  - 在 `const { Content } = await render(article);` 之後新增：
```ts
const topicTitle = new Map((await getTopics()).map((t) => [t.id, t.data.title]));
const aboutNames = article.data.topics
  .map((s) => topicTitle.get(s))
  .filter((x): x is string => Boolean(x));
const authorImageAbs = author
  ? new URL(authorAvatar(author), Astro.site).toString()
  : undefined;
```
  - 把 `articleLd(Astro.site, {...})` 呼叫改為：
```ts
  articleLd(Astro.site, {
    headline: article.data.title,
    description: article.data.description,
    path,
    image: new URL(cover, Astro.site).toString(),
    datePublished: isoDate(article.data.publishDate),
    dateModified: isoDate(article.data.updatedDate ?? article.data.publishDate),
    author: {
      name: authorName,
      path: authorHasPage(author) ? `/authors/${author!.id}/` : undefined,
      image: authorImageAbs,
      jobTitle: author?.data.displayTitle,
      sameAs: author?.data.socialLinks.map((s) => s.url),
    },
    section: cat?.name,
    isNews: isMedicalOrNews,
    keywords: article.data.tags,
    about: aboutNames,
  }),
```

- [ ] **Step 6: build 驗證** — `pnpm build`。Expected: 成功。隨機挑一篇文章頁確認 JSON-LD 含 `"@type":"Person"` 的 author 與 `keywords`：
```bash
f=$(find dist/articles -name index.html | head -1); grep -o '"author":{"@type":"Person"[^}]*}' "$f" | head -1; grep -o '"keywords":"[^"]*"' "$f" | head -1
```
Expected: 印出 author Person 物件與 keywords 字串。

- [ ] **Step 7: Commit:**
```bash
git add src/utils/jsonld.ts src/utils/jsonld.test.ts "src/pages/articles/[slug].astro"
git commit -m "feat(aeo): Article JSON-LD 完整 Person author + keywords + about"
```

---

## Task 5: SEO meta 補強（author / article:author / keywords / twitter）

**Files:**
- Modify: `src/config/site.ts`、`src/components/seo/SEOHead.astro`、`src/layouts/BaseLayout.astro`、`src/pages/articles/[slug].astro`

- [ ] **Step 1: SITE 加可選 X handle** — 在 `src/config/site.ts` 的 `SITE` 物件內、`org` 之前加入：
```ts
  /** Twitter/X 帳號（含 @；未設定則不輸出 twitter:site/creator） */
  x: '',
```

- [ ] **Step 2: SEOHead 加 props 與 meta** — `src/components/seo/SEOHead.astro`：
  - 在 `interface Props` 加：
```ts
  authorName?: string;
  authorPath?: string;
  keywords?: string[];
```
  - 解構 `Astro.props` 時加入 `authorName`、`authorPath`、`keywords`。
  - 在 `<meta name="description" ... />` 之後加：
```astro
{authorName && <meta name="author" content={authorName} />}
{keywords && keywords.length > 0 && <meta name="keywords" content={keywords.join(', ')} />}
```
  - 在 `{modifiedTime && ...}` 之後（OG 區塊內）加：
```astro
{authorPath && <meta property="article:author" content={absoluteUrl(authorPath, Astro.site)} />}
```
  - 在 Twitter 區塊（`twitter:image` 之後）加：
```astro
{SITE.x && <meta name="twitter:site" content={SITE.x} />}
{SITE.x && <meta name="twitter:creator" content={SITE.x} />}
```

- [ ] **Step 3: BaseLayout 轉傳** — `src/layouts/BaseLayout.astro`：
  - `interface Props` 加 `authorName?: string; authorPath?: string; keywords?: string[];`
  - 解構加入這三個。
  - `<SEOHead ... />` 加上 `authorName={authorName} authorPath={authorPath} keywords={keywords}`。

- [ ] **Step 4: 文章頁傳入** — `src/pages/articles/[slug].astro` 的 `<BaseLayout ...>` 加：
```astro
  authorName={authorName}
  authorPath={authorHasPage(author) ? `/authors/${author!.id}/` : undefined}
  keywords={article.data.tags}
```

- [ ] **Step 5: build 驗證** — `pnpm build`。Expected: 成功。挑一篇文章頁：
```bash
f=$(find dist/articles -name index.html | head -1); grep -o '<meta name="author"[^>]*>' "$f"; grep -o '<meta name="keywords"[^>]*>' "$f"; grep -o '<meta property="article:author"[^>]*>' "$f"
```
Expected: 三個 meta 都出現（twitter:site/creator 因 `SITE.x` 為空不輸出，符合預期）。

- [ ] **Step 6: Commit:**
```bash
git add src/config/site.ts src/components/seo/SEOHead.astro src/layouts/BaseLayout.astro "src/pages/articles/[slug].astro"
git commit -m "feat(aeo): SEO meta 補 author/article:author/keywords/twitter"
```

---

## Task 6: RSS 全文 + 作者

**Files:**
- Modify: `src/pages/rss.xml.ts`、`package.json`（新增依賴）

- [ ] **Step 1: 安裝依賴** — `pnpm add markdown-it sanitize-html` 與型別 `pnpm add -D @types/markdown-it @types/sanitize-html`。確認 `pnpm-lock.yaml` 更新。

- [ ] **Step 2: 改 `src/pages/rss.xml.ts`** — 全檔替換為：
```ts
import rss from '@astrojs/rss';
import type { APIContext } from 'astro';
import MarkdownIt from 'markdown-it';
import sanitizeHtml from 'sanitize-html';
import { SITE } from '@/config/site';
import { getPublishedArticles, getAuthorMap, articleSlug } from '@/utils/content';
import { url, absoluteUrl } from '@/utils/url';

const parser = new MarkdownIt({ html: true });

/** 把內文 root-relative 的 /images、/covers 等資產改成絕對網址（feed reader 需要） */
function absolutizeAssets(html: string, site: URL | string | undefined): string {
  if (!site) return html;
  return html.replace(/(src|href)="(\/[^"]*)"/g, (_m, attr, path) => {
    if (path.startsWith('//')) return `${attr}="${path}"`;
    return `${attr}="${absoluteUrl(path, site)}"`;
  });
}

export async function GET(context: APIContext) {
  const site = context.site;
  const articles = await getPublishedArticles();
  const authorMap = await getAuthorMap();
  return rss({
    title: `${SITE.name}｜${SITE.tagline}`,
    description: SITE.description,
    site: site ?? SITE.taglineEn,
    items: articles.slice(0, 30).map((a) => {
      const author = authorMap.get(a.data.author);
      const html = sanitizeHtml(parser.render(a.body ?? ''), {
        allowedTags: sanitizeHtml.defaults.allowedTags.concat(['img', 'figure', 'figcaption']),
        allowedAttributes: {
          ...sanitizeHtml.defaults.allowedAttributes,
          img: ['src', 'alt', 'width', 'height', 'loading', 'decoding'],
        },
      });
      return {
        title: a.data.title,
        description: a.data.description,
        pubDate: a.data.publishDate,
        link: url(`/articles/${articleSlug(a)}/`),
        categories: a.data.tags,
        author: author?.data.name ?? 'APPI 編輯部',
        content: absolutizeAssets(html, site),
      };
    }),
    customData: `<language>zh-Hant</language>`,
  });
}
```

- [ ] **Step 3: build 驗證** — `pnpm build`。Expected: 成功。檢查 RSS 有 author 與全文 content：
```bash
grep -q "<author>" dist/rss.xml && echo author-ok
grep -q "content:encoded" dist/rss.xml && echo content-ok
```
Expected: `author-ok`、`content-ok`。若 build 因全文失敗或過慢（spec §F 備案），退為 `content: ` 用 `description` + highlights 組的摘要 HTML，並於報告說明。

- [ ] **Step 4: Commit:**
```bash
git add src/pages/rss.xml.ts package.json pnpm-lock.yaml
git commit -m "feat(aeo): RSS 加作者與全文 content"
```

---

## Task 7: 作者頁 UX（website / email / joinedDate）

**Files:**
- Modify: `src/pages/authors/[slug].astro`

- [ ] **Step 1: 在 author-hero-info 補連結與日期** — 在 `src/pages/authors/[slug].astro` 的 `.author-specialties` 區塊之後（仍在 `<div class="author-hero-info">` 內）加：
```astro
        <div class="author-meta">
          {author.data.website && (
            <a class="author-link" href={author.data.website} rel="me noopener" target="_blank">官方網站</a>
          )}
          {author.data.emailPublic && (
            <a class="author-link" href={`mailto:${author.data.emailPublic}`}>{author.data.emailPublic}</a>
          )}
          {author.data.joinedDate && (
            <span class="author-joined">加入於 {author.data.joinedDate.getFullYear()} 年</span>
          )}
        </div>
```

- [ ] **Step 2: 加樣式** — 在 `<style>` 內（`.author-specialties span {…}` 之後）加：
```css
  .author-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 12px;
    margin-top: 10px;
    font-size: var(--text-xs);
    font-family: var(--font-sans);
  }
  .author-link {
    color: var(--appi-brand);
    font-weight: 600;
  }
  .author-joined {
    color: var(--ink-3);
  }
```

- [ ] **Step 3: build 驗證** — `pnpm build`。Expected: 成功。確認 lightman 作者頁有官方網站連結：
```bash
f=dist/authors/lightman/index.html; grep -q 'yao.care' "$f" && echo website-ok
```
Expected: `website-ok`（lightman 的 website 為 https://yao.care/）。

- [ ] **Step 4: Commit:**
```bash
git add "src/pages/authors/[slug].astro"
git commit -m "feat(aeo): 作者頁顯示 website/email/joinedDate"
```

---

## Task 8: newsroom 連動（tags + FAQ）

**Files:**
- Modify: `.claude/skills/newsroom/SKILL.md`

- [ ] **Step 1: 在「硬規則總結」區塊補一條** — 在 `.claude/skills/newsroom/SKILL.md` 的 `## 硬規則總結` 清單最後加：
```markdown
- AEO：每篇務必填 `tags`（餵 keywords 與 RSS／llms 索引）；題目適合時設「常見問題」H2 以觸發 FAQPage 結構化資料。
```

- [ ] **Step 2: Commit:**
```bash
git add .claude/skills/newsroom/SKILL.md
git commit -m "feat(aeo): newsroom 連動 tags 與 FAQ 結構化資料"
```

---

## Task 9: 整體建置與連結 gate

**Files:** 無（驗收）

- [ ] **Step 1: 全量 build + 連結 gate** — `pnpm build && pnpm check:links`。Expected: build 成功、check:links 全綠。

- [ ] **Step 2: 產物總檢**：
```bash
for f in dist/llms.txt dist/llms-full.txt dist/robots.txt dist/rss.xml; do test -f "$f" && echo "ok $f"; done
```
Expected: 四個皆 `ok`。

- [ ] **Step 3: JSON-LD 驗證提示** — 部署後（push 觸發），用 Google Rich Results Test / schema.org validator 抽驗一篇文章頁 URL：確認 Article/NewsArticle（含完整 Person author、keywords）、BreadcrumbList、FAQPage（如有）無錯誤。記錄結果。

- [ ] **Step 4（部署後）效能複測** — 依 `PERFORMANCE.md` 用第三方 PSI 抽驗線上頁面，確認分數不退（本案幾乎不影響 render-blocking）。

---

## Self-Review（撰寫者自檢）

**1. Spec 覆蓋：** A=Task1+2；B=Task1+2；C=Task3；D=Task4；E=Task5；F=Task6；G=Task7；H=Task8；驗收=Task9。全部對應。

**2. Placeholder 掃描：** 程式步驟均附完整程式碼與預期輸出；無 TBD/TODO；驗證指令具體。

**3. 型別一致：** `articleLd` 新簽章（`author` 物件、`keywords`、`about`）在 Task4 定義並同任務更新呼叫端，測試一致；SEOHead/BaseLayout 新 props（authorName/authorPath/keywords）三處一致；`buildLlmsTxt`/`buildLlmsFullTxt` 簽章在 Task1 定義、Task2 依此呼叫。

**4. 待實作時確認的相依：** `isoDate`（`@/utils/date`）已於文章頁使用，Task2 沿用；`getTopics`、`authorAvatar` 來自 `@/utils/content`（已存在）；topics 的 `data.title` 欄位存在（topics collection 必填 title）。若 `isoDate` 簽章不符，取其輸出前 10 碼為日期。
