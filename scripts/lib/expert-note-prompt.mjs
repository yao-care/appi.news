/**
 * 署名人格文章的 `expertNote`（文末「○○○的判讀」區塊）寫作指示。
 *
 * 適用範圍：**只有 author 是具名個人的文章**（luo-yang／huang-ziyan／lightman／
 * wu-fang-jun／chou-jingyan）。`author: "appi-editorial"` 的組織署名文章不寫這欄——
 * 沒有一個具體的人在下判斷，硬寫就是無主的意見。
 *
 * 為什麼可以由 LLM 起草（2026-08-02 站長裁定）：文章本來就掛在該具名作者名下、
 * 由本人審閱，署名本身的宣稱強度不比一段判讀低。既然接受署名代表本人背書，
 * 在同一篇裡寫出他的判讀就不是額外的假造。
 *
 * 但守一條線：**不得虛構個人經歷**。可以寫從內文與其專業領域推得的判斷，
 * 不可以編造「我在門診看過」「我經手的案子裡」這種第一手軼事——那是署名承擔不了的宣稱。
 *
 * 三個文末區塊的分工（寫重複就沒有存在意義）：
 *   本文重點 highlights      → 這篇講了什麼（事實摘要）
 *   ○○○的判讀 expertNote     → 所以呢：對誰重要、實務上該怎麼看
 *   風險與限制 risksAndLimits → 這個結論的邊界在哪
 */
export const EXPERT_NOTE_PROMPT = [
  '【判讀】若本篇 author 是具名個人（不是 appi-editorial），frontmatter 一併填 `expertNote`：',
  '以該作者的專業視角，用 1~3 句、合計 ≤120 字，回答「所以呢」——',
  '這件事對誰重要、實務上該怎麼看或怎麼做、哪個環節最該留意。',
  '要與 `highlights`（講了什麼）和 `risksAndLimits`（邊界在哪）明確區隔，不要覆述兩者。',
  '- **禁對比句式**：不要寫「不是X，而是Y」「不只是X，更是Y」「並非X，而是Y」這類定義句，也不要用「真正該X的不是A而是B」。直述你的判斷即可（這是 check-content 的 ERROR，會擋 build）。',
  '**不得虛構個人經歷**：不可寫「我在門診看過」「我經手的案子」這類第一手軼事，',
  '只能寫從內文與該作者專業領域推得的判斷。繁中台灣用語、禁破折號、禁 AI 套語。',
  '`author: "appi-editorial"` 的組織署名文章**不要填這欄**。',
].join('');
