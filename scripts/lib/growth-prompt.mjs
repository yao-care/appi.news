// 成長規則 prompt 片段：所有「會產出文章」的產線共用同一份正本。
//
// 為什麼要有這支：站台已經通過「Google 願意收錄並給流量」那一關，接下來的瓶頸不在產量，
// 在三件寫作當下就能決定、事後補很貴的事——站內導流、可被點擊的標題、可被引用的開頭。
// 完整脈絡與工作項目＝docs/growth-playbook.md，為什麼＝docs/lessons/growth-three-gates.md。
//
// 用法：在各線的起草 prompt 陣列裡插入 GROWTH_PROMPT（與 RISKS_PROMPT／EXPERT_NOTE_PROMPT 同層級）。
// 已接的產線清單見 docs/growth-playbook.md §產線接線點；新增產線務必一併接上。
//
// 🔴 這段是「寫作規範」不是「現況」：不要在裡面寫任何會變的數字（篇數、topic 數、分數）。

export const GROWTH_PROMPT = [
  '【成長規則（全站產線共用，寫作當下就要做完，事後補很貴）】',
  'G1. **站內導流：內文至少 2 條指向站上既有文章的超連結，其中至少 1 條要落在文章前三分之一。**',
  '    - 為什麼在前三分之一：多數讀者從搜尋結果進來看完前幾段就走，文末的「延伸閱讀」區塊救不到他們。',
  '    - 格式 `[看得出對方在講什麼的錨文字](/articles/<slug>/)`，**禁止**「這裡」「點此」「詳見本站」這種錨文字。',
  '    - **只能連你親手確認存在的檔案**。做法：先 `ls src/content/articles/ | grep <關鍵字>` 或',
  '      `grep -rl "<關鍵詞>" src/content/articles/ | head` 找出真實 slug，再 `head -8 src/content/articles/<slug>.md`',
  '      確認題目確實扣得上你要連的那句話。',
  '    - **查不到合適的就少連甚至不連，絕對不要憑印象拼 slug**——連到不存在的頁會讓 check:links 失敗、整篇不發佈。',
  'G2. **`topics` / `column` 一定要判斷過再決定填不填。**',
  '    - `ls src/content/topics/` 看有哪些主題叢集，本篇扣得上就填 frontmatter `topics: ["<id>", …]`。',
  '    - `ls src/content/columns/` 看有哪些專欄，本篇屬於某個專欄就填 `column: "<id>"`。',
  '    - 這兩個欄位是站內「延伸閱讀」排序的最高權重（同 topic 每個 ×3、同 column +3，遠高於同分類 +2），',
  '      沒填等於自廢站內導流，讓系統只能退回「同分類最新文」這種不相干的推薦。',
  '    - 扣不上就空著，**不要硬填也不要自創沒有的 id**（自創會讓主題頁連結壞掉）。',
  'G3. **title 與 description 是寫給「搜尋結果頁」的，不是寫給自己看的。**',
  '    - title：讀者真的會搜的詞放前段，全形 30 字內把重點講完（更長會在搜尋結果被截斷、看不到重點）。',
  '    - description：60–160 字，寫「讀完能得到什麼、拿到哪些具體數字或做法」，不要照抄 title 那句話。',
  'G4. **開頭直接回答**：第一段前兩句就要把核心結論講完，不要先鋪陳背景再說結論。',
  '    搜尋引擎的摘要與 AI 助理的引用都從前段抓，鋪陳寫在前面等於把被引用的機會讓給別人。',
  'G5. 題目能整理成問答的話，文末補一個 `## 常見問題` 區段（3 題以內，一問一答，答案至少兩句）。',
  '    這個區段會被抽成 FAQPage 結構化資料，是拿搜尋結果版位與被 AI 引用的低成本手段。',
].join('\n');

export default GROWTH_PROMPT;
