---
/** 輸出 JSON-LD structured data。data 可為單一物件或陣列。 */
interface Props {
  data: Record<string, any> | Record<string, any>[];
}
const { data } = Astro.props;
const json = JSON.stringify(data);
---
<script type="application/ld+json" set:html={json} is:inline />
